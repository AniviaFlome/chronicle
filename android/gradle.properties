org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Built-in Kotlin is enabled: the app applies no KGP itself and every
# Android plugin (flutter_timezone, android_file_picker, plus.community,
# url_launcher, permission_handler) builds with AGP 9-provided Kotlin.
# Requires Flutter 3.47+.
android.builtInKotlin=true
